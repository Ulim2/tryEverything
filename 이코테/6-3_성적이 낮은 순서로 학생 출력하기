n = int(input())

arr = []
for i in range(n):
    a, b = map(str, input().split())
    arr.append((a, int(b)))

arr.sort(key = lambda arr: arr[1])

for i in range(len(arr)):
    print(arr[i][0], end = ' ')
